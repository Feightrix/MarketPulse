name: MarketPulse Options Research

on:
  push:
    branches: [main]
    paths:
      - options_pattern1_refinement.py
      - options_pattern1_geometry.py
      - options_pattern1_geometry_eval.py
      - options_pattern2_vwap_reversion.py
      - .github/workflows/options-pattern1-refinement.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: options-research
  cancel-in-progress: false

jobs:
  options-research:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - name: Checkout current main
        uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Security check — research only
        run: |
          python - <<'PY'
          from pathlib import Path
          base = Path('options_pattern1_backtest.py').read_text()
          p1 = Path('options_pattern1_refinement.py').read_text()
          p2 = Path('options_pattern2_vwap_reversion.py').read_text()
          assert 'ALPACA_OPTIONS_API_KEY_ID' in base
          assert 'https://data.alpaca.markets' in base
          assert 'order_submission_enabled' in p1
          assert 'order_submission_enabled' in p2
          assert 'paper-api.alpaca.markets/v2/orders' not in p2
          assert 'api.alpaca.markets/v2/orders' not in p2
          print('Options research-only invariants: PASS')
          PY

      - name: Syntax check
        run: |
          python -m py_compile options_pattern1_backtest.py
          python -m py_compile options_pattern1_refinement.py
          python -m py_compile options_pattern2_vwap_reversion.py

      - name: Run Pattern 1 leader refresh
        env:
          ALPACA_OPTIONS_API_KEY_ID: ${{ secrets.ALPACA_OPTIONS_API_KEY_ID }}
          ALPACA_OPTIONS_SECRET_KEY: ${{ secrets.ALPACA_OPTIONS_SECRET_KEY }}
        run: python options_pattern1_refinement.py

      - name: Run Pattern 2 VWAP reversion
        env:
          ALPACA_OPTIONS_API_KEY_ID: ${{ secrets.ALPACA_OPTIONS_API_KEY_ID }}
          ALPACA_OPTIONS_SECRET_KEY: ${{ secrets.ALPACA_OPTIONS_SECRET_KEY }}
        run: python options_pattern2_vwap_reversion.py

      - name: Validate Pattern 2
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          result = json.loads(Path('options_pattern2_results.json').read_text())
          assert result['order_submission_enabled'] is False
          assert result['strategy'] == 'options_pattern2_vwap_stretch_reversal'
          assert result['complete_sessions'] > 20
          print('PATTERN 2 FOLD A', json.dumps(result['fold_a'], indent=2))
          print('PATTERN 2 FOLD B', json.dumps(result['fold_b'], indent=2))
          print('PATTERN 2 HOLDOUT', json.dumps(result['holdout'], indent=2))
          print('PATTERN 2 FULL', json.dumps(result['full_sample'], indent=2))
          print('PATTERN 2 CALLS', json.dumps(result['direction_split_full']['calls'], indent=2))
          print('PATTERN 2 PUTS', json.dumps(result['direction_split_full']['puts'], indent=2))
          PY

      - name: Commit research results
        if: always()
        run: |
          git config user.name "MarketPulse Bot"
          git config user.email "actions@users.noreply.github.com"
          mkdir -p /tmp/options-research
          for f in options_pattern1_refinement_results.json options_pattern1_refinement_results.md options_pattern2_results.json options_pattern2_results.md; do
            if [ -f "$f" ]; then cp "$f" "/tmp/options-research/$f"; fi
          done
          git fetch origin main
          git reset --hard origin/main
          for f in options_pattern1_refinement_results.json options_pattern1_refinement_results.md options_pattern2_results.json options_pattern2_results.md; do
            if [ -f "/tmp/options-research/$f" ]; then
              cp "/tmp/options-research/$f" "$f"
              git add "$f"
            fi
          done
          if git diff --cached --quiet; then
            echo "No options research result changes"
          else
            git commit -m "Update Options Pattern 2 research"
            git push origin main
          fi
