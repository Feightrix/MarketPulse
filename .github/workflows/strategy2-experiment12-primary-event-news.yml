name: Strategy 2 Experiment 12 Primary Event News

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - strategy2_experiment12_primary_event_news.py
      - .github/workflows/strategy2-experiment12-primary-event-news.yml

permissions:
  contents: write

concurrency:
  group: strategy2-experiment12-primary-event-news
  cancel-in-progress: true

jobs:
  research:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - run: pip install numpy pandas requests
      - name: Verify locked protocol
        run: |
          python - <<'PY'
          from pathlib import Path
          s=Path('strategy2_experiment12_primary_event_news.py').read_text()
          b=Path('strategy2_experiment10_entity_abnormal_news.py').read_text()
          assert 'only_changed_variable": "event_type_and_novelty_classifier"' in s
          assert 'BROKER_ORDERS = False' in s and 'LONG_ONLY = True' in s and 'LEVERAGE = False' in s
          assert '/v2/orders' not in s+b and 'paper-api.alpaca.markets' not in s+b
          assert 'MIN_MOVE_Z = 1.75' in b and 'MIN_VOLUME_Z = 1.00' in b and 'MIN_RS_Z = 1.25' in b
          assert 'COST_BPS_PER_FILL = 10.0' in b and 'RISK_PER_TRADE_PCT = 0.0075' in b and 'MAX_NOTIONAL_PCT = 0.50' in b
          assert 'NONBLIND_FOLLOWUP = True' in s
          print('Experiment 12 invariants: PASS')
          PY
      - run: python -m py_compile strategy2_experiment10_entity_abnormal_news.py strategy2_experiment12_primary_event_news.py
      - name: Run backtest
        env:
          ALPACA_STRATEGY2_API_KEY_ID: ${{ secrets.ALPACA_STRATEGY2_API_KEY_ID }}
          ALPACA_STRATEGY2_SECRET_KEY: ${{ secrets.ALPACA_STRATEGY2_SECRET_KEY }}
        run: python strategy2_experiment12_primary_event_news.py
      - name: Commit results
        run: |
          git config user.name "MarketPulse Bot"
          git config user.email "actions@users.noreply.github.com"
          mkdir -p /tmp/s2e12
          for f in strategy2_experiment12_primary_event_news_results.json strategy2_experiment12_primary_event_news_summary.md; do
            if [ -f "$f" ]; then cp "$f" "/tmp/s2e12/$f"; fi
          done
          git fetch origin main
          git reset --hard origin/main
          for f in strategy2_experiment12_primary_event_news_results.json strategy2_experiment12_primary_event_news_summary.md; do
            if [ -f "/tmp/s2e12/$f" ]; then cp "/tmp/s2e12/$f" "$f"; git add "$f"; fi
          done
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Record primary-event news momentum results"
          git push origin main

# trigger-marker: classifier and all trading rules unchanged
